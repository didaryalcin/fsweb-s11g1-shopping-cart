/*
context oluştur
içine kullanmak istediklerini at
kullanmak istediğin yerde kullanmak istediklerini kullan
*/

import { createContext } from "react";

export const ProductContext = createContext();
export const CartContext = createContext();
